apply plugin: 'com.github.dcendents.android-maven'
apply plugin: 'com.jfrog.bintray'

def GROUP = 'org.bbs.android'
// sync with Readme.md
def VERSION = '0.1'

group = GROUP
version = VERSION

install {
    repositories.mavenInstaller {
        pom.project {
            name 'android-focus-path-m'
            description 'focus path manger for andorid'
            url 'https://github.com/luoqii/android_focus_path_manager'
            //inceptionYear '2015'

            packaging 'aar'
            groupId GROUP

            // must math lib module's name. shitt!!!
            artifactId 'focus-path-m'
            version VERSION

            licenses {
                license {
                    name 'The Apache Software License, Version 2.0'
                    url 'http://www.apache.org/licenses/LICENSE-2.0.txt'
                    distribution 'repo'
                }
            }
            scm {
                connection 'https://github.com/luoqii/android_focus_path_manager.git'
                url 'https://github.com/luoqii/android_focus_path_manager'

            }
            developers {
                developer {
                    name 'bb.s'
                }
            }
        }
    }
}

task sourcesJar(type: Jar) {
    from android.sourceSets.main.java.srcDirs
    classifier = 'sources'
}

task javadoc(type: Javadoc) {
    source = android.sourceSets.main.java.srcDirs
    classpath += project.files(android.getBootClasspath().join(File.pathSeparator))
    source += "$buildDir/generated/source/r/debug/org/bb/android/library/R.java"
}

task javadocJar(type: Jar, dependsOn: javadoc) {
    classifier = 'javadoc'
    from javadoc.destinationDir
}

artifacts {
    archives javadocJar
    archives sourcesJar
}

Properties properties = new Properties()
properties.load(project.rootProject.file('local.properties').newDataInputStream())
bintray {
    user = properties.getProperty("bintray.user")
    key = properties.getProperty("bintray.apikey")

    publish = true
    configurations = ['archives']
    pkg {
        repo = 'maven'
        name = 'android-focus-path-m'
        userOrg = user
        licenses = ['Apache-2.0']
        vcsUrl = 'https://github.com/luoqii/android_focus_path_manager.git'
        version {
            name = VERSION
            desc = 'init version'
            vcsTag = '0.1'
            attributes = ['gradle-plugin': 'com.use.less:com.use.less.gradle:gradle-useless-plugin']
        }
    }
}

//task wrapper(type: Wrapper) {
//  gradleVersion = '2.4'
//}